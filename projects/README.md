# First-class projects

Every immediate directory under this path is one first-class `moldea` package project with a stable responsibility, package identity, public or private boundary, README, implementation, and tests.

Add a project only when its catalog entry, dependency direction, distribution status, and focused specification are established. The project catalog describes approved architecture rather than implementation status, so planned packages do not receive placeholder directories or manifests.
