# Claude Agent SDK adapter fixture

This fixture exercises the verified TypeScript query wrapper, programmatic subagent, instruction-loader, JSON Schema output, SDK MCP tool, and routing-description relationships through the real Core and memory-repository boundary. It contains no credentials and never executes the SDK integration.
