# Cloudflare Agents adapter fixtures

The fixture covers both verified Cloudflare targets, direct instruction and schema relationships, one repository-local AI SDK function tool, and one `agentTool` handoff.
