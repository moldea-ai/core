# OpenAI Agents SDK adapter fixture

This fixture exercises the verified TypeScript Agent, function-tool, schema, instruction-loader, and handoff target through the real Core and memory-repository boundary. It contains no credentials and never executes the SDK integration.
